# AGENTS.md

## 1. 项目说明

本项目是黄柏霖的个人博客与作品集网站，主要用于：

* 展示个人项目与作品
* 记录学习、开发和实践经历
* 发布技术文章
* 展示技能栈和个人方向
* 用于实习、求职、项目答辩和个人品牌建设

网站定位不是传统的纯文章博客，而是以“个人作品与经历”为核心、博客内容为补充的个人网站。

项目需要保持真实、克制、专业。不得虚构用户没有提供的教育经历、实习职位、奖项、项目指标、公司合作、论文成果或技术能力。

---

## 2. 用户信息

姓名：

* 中文：黄柏霖
* 英文：Huang Bolin

当前身份：

* 北京信息科技大学自动化专业学生

主要关注方向：

* 人工智能
* 计算机视觉
* 具身智能
* 机器人控制
* AIoT
* 工业自动化
* 全栈开发

个人定位：

> 自动化专业学生，关注人工智能、机器人、计算机视觉和工业自动化。喜欢通过完整项目验证想法，从产品设计、前端开发、后端服务到算法与系统集成，持续探索软件与现实世界的连接。

修改个人介绍时，可以优化表达，但不得增加未经确认的事实。

---

## 3. 核心项目

网站首批展示以下项目。

### AIoT 智慧温室种植系统原型

类型：

* AIoT
* 工业自动化
* 智慧农业

主要技术：

* React
* TypeScript
* Vite
* Vitest
* Playwright
* Dexie
* Canvas

主要能力：

* 环境数据模拟与监测
* 自动和手动控制
* 报警中心
* 设备管理
* AI 识别展示
* 快照导入导出
* CSV 数据导出
* 答辩演示模式
* simulation、external、playback 三种运行类型
* 为工业网关和外部设备预留接口

项目边界：

这是一个可用于答辩演示的 MVP，目前以模拟数据和接口预留为主。

不得描述为已经完成真实工业现场部署，也不得声称已经接入真实研华设备。

### Embodied Training Platform

类型：

* 具身智能
* 机器人教学
* 全栈开发

主要技术：

* React
* TypeScript
* FastAPI
* Python

主要能力：

* 二维机器人仿真
* BFS 避障路径规划
* 目标抓取
* 任务状态机
* 自动任务执行
* 前后端状态同步
* 规则化指令解析

项目边界：

这是一个个人教学仿真项目，不是真实机器人控制平台。

### SIGNAL-HUNT

类型：

* 活动系统
* 桌面应用
* 交互设计

主要技术：

* Electron
* React
* TypeScript

主要能力：

* Staff 现场控制端
* Admin 运营管理端
* Display 大屏展示端
* 抽奖过程动画
* 多窗口状态恢复
* 异常处理
* 响应式设计
* reduced-motion 支持

### Finding Job

类型：

* 求职工具
* 信息导航
* 前端产品

主要能力：

* 公司与岗位信息展示
* 筛选与排序
* 技能矩阵
* 分享链接
* 数据完整性检查

项目边界：

目前是静态求职导航网站，主要用于信息整理和产品验证。

### SNN 社团官网

类型：

* 社团官网
* 品牌设计
* 前端开发

主要能力：

* 社团介绍
* 项目展示
* 公众号入口
* 科技风品牌视觉
* 响应式网页设计

---

## 4. 默认技术方案

除非现有项目已经采用其他成熟方案，否则优先使用：

* Next.js
* TypeScript
* App Router
* Tailwind CSS
* Markdown 或 MDX
* ESLint
* Prettier

优先采用：

* Server Components
* 静态生成
* 类型安全的数据结构
* 本地内容文件
* 可复用组件
* 轻量 CSS 动画
* SEO 友好的页面结构

初期不要引入：

* 数据库
* 用户系统
* 评论系统
* 管理后台
* 复杂 CMS
* 实时通信
* 支付功能
* 重型 UI 组件库
* 无明确用途的状态管理框架

只有当现有需求无法通过当前技术栈合理实现时，才允许增加新依赖。

增加依赖前必须说明：

1. 依赖解决的问题
2. 为什么现有能力无法完成
3. 对构建体积和维护成本的影响

---

## 5. 内容架构

内容和页面逻辑必须分离。

推荐结构：

```text
src/
  app/
  components/
  data/
  lib/
  styles/
  types/

content/
  blog/
  projects/

public/
  images/
    projects/
    blog/
    profile/
```

推荐数据文件：

```text
src/data/profile.ts
src/data/projects.ts
src/data/experience.ts
src/data/skills.ts
src/data/navigation.ts
src/data/contact.ts
```

项目、经历、技能和联系方式不得分散硬编码在页面组件中。

新增或修改内容时，应优先修改数据文件或 Markdown/MDX 文件，而不是修改页面结构。

---

## 6. 页面要求

网站至少包含：

```text
/
/projects
/projects/[slug]
/experience
/blog
/blog/[slug]
/about
/contact
```

### 首页

首页优先展示：

1. 个人身份
2. 关注方向
3. 精选项目
4. 技能矩阵
5. 最近经历
6. 最新文章
7. 联系方式

首屏需要清楚回答：

* 我是谁
* 我在做什么
* 我希望进入什么方向

避免使用空洞口号，例如：

* 改变世界
* 创造无限可能
* 探索未来
* 重新定义体验
* 用技术赋能一切

### 项目页面

项目详情必须明确区分：

* 项目背景
* 要解决的问题
* 主要功能
* 技术方案
* 用户承担的工作
* 当前状态
* 已知限制
* 后续计划

没有提供的信息使用“待补充”，不得自行编造。

### 博客页面

博客内容使用 Markdown 或 MDX。

每篇文章至少包含：

* title
* description
* date
* updated
* tags
* category
* draft
* cover

示例文章必须明确标记为示例或草稿，不得伪装成已经正式发布的文章。

### 联系页面

联系表单在没有接入真实发送服务时：

* 不得显示“发送成功”
* 不得伪造网络请求
* 应明确提示当前为演示表单
* 可以提供复制邮箱或跳转邮件客户端功能

---

## 7. 视觉规范

设计关键词：

* 瑞士设计
* 编辑排版
* 工程感
* 实验室感
* 清晰网格
* 大面积留白
* 低饱和度
* 精确的信息层级

推荐表现：

* 浅色背景
* 深灰正文
* 单一强调色
* 编号式项目索引
* 细线分隔
* 技术标签矩阵
* 编辑式大标题
* 项目状态标记
* 技术档案式项目详情

避免：

* 大量渐变
* 大面积玻璃拟态
* 无意义发光
* 满屏粒子
* 所有内容都放入圆角卡片
* 过大的圆角
* 过重阴影
* 过度悬浮动画
* 模板化 SaaS 首页
* 赛博朋克式装饰
* 内容密度过低
* 纯装饰性数据图表

动画必须服务于：

* 页面状态变化
* 信息层级
* 页面切换
* 用户操作反馈

不得为了“高级感”增加无意义动画。

---

## 8. 响应式要求

重点检查以下宽度：

* 1440px
* 1024px
* 768px
* 390px

必须保证：

* 移动端导航可用
* 页面无横向溢出
* 标题不会异常截断
* 项目卡片内容完整
* 代码块可以横向滚动
* 图片保持正确比例
* 触控目标尺寸合理
* 筛选器在手机端可操作

不能仅通过缩小字号适配移动端，应重新组织布局。

---

## 9. 无障碍要求

必须实现：

* 语义化 HTML
* 键盘导航
* 可见的 focus 状态
* 图片 alt
* 合理颜色对比度
* 表单 label
* 合理标题层级
* `prefers-reduced-motion`
* 不依赖 hover 才能获取的信息
* 图标按钮具有可访问名称

禁止为了视觉效果移除 outline，除非提供清晰的替代 focus 样式。

---

## 10. SEO 要求

必须包含：

* 全局 metadata
* 页面级 metadata
* 项目详情 metadata
* 博客详情 metadata
* Open Graph
* Twitter Card
* sitemap
* robots.txt
* canonical URL 配置入口
* favicon
* 404 页面

页面标题格式建议：

```text
页面名称 | 黄柏霖
```

英文站点名称：

```text
Huang Bolin — Portfolio & Notes
```

结构化数据可以包含：

* Person
* WebSite
* BlogPosting
* CreativeWork

不得在结构化数据中加入未提供的组织关系、职位或获奖信息。

---

## 11. 代码规范

### TypeScript

* 开启严格模式
* 避免使用 `any`
* 公共数据结构必须定义类型
* 外部数据必须校验
* 不得通过类型断言掩盖真实问题

### React

* 优先使用 Server Components
* 只有需要交互或浏览器 API 时才使用 `"use client"`
* 避免不必要的 `useEffect`
* 不要复制可由 props 或数据计算得出的状态
* 列表必须使用稳定 key
* 不得在渲染过程中产生副作用

### 组件

组件应围绕明确职责设计。

推荐组件：

* Header
* MobileNavigation
* Footer
* Container
* SectionHeading
* ProjectCard
* ProjectGrid
* ProjectFilter
* TechTag
* StatusBadge
* ExperienceTimeline
* SkillMatrix
* BlogCard
* TableOfContents
* ContactLinks
* EmptyState
* BackLink

不要为了抽象而抽象。

以下情况通常不需要独立组件：

* 只包装一个 `div`
* 只使用一次且逻辑简单
* 没有独立语义
* 没有复用价值
* 拆分后反而难以理解

### 样式

* 优先复用设计 token
* 不要在多个页面重复相同魔法数
* 保持一致的间距、字号、圆角和边框
* 不要随意添加局部颜色
* 深色模式只有在整体完成度足够时再增加

---

## 12. 数据规则

项目 slug 必须：

* 唯一
* 使用小写英文
* 使用连字符分隔
* 不包含空格
* 不依赖标题动态生成

项目状态使用统一枚举，例如：

```ts
type ProjectStatus =
  | "concept"
  | "in-progress"
  | "mvp"
  | "completed"
  | "maintained"
  | "archived";
```

链接字段允许为空。

当 GitHub 或 Demo 链接为空时：

* 不渲染可点击按钮
* 可以显示“暂未公开”
* 不得使用 `#` 作为假链接

日期必须使用明确格式，避免混用字符串格式。

---

## 13. Git 与文件安全

开始修改前必须检查：

```bash
git status
git branch --show-current
git log -1 --oneline
```

如果目录不是 Git 仓库，要明确说明。

不得执行：

```bash
git reset --hard
git clean -fd
git checkout -- .
git restore .
```

除非用户明确要求，否则不得：

* 删除用户原有文件
* 重写 Git 历史
* 强制推送
* 修改远程仓库地址
* 提交包含密钥的文件
* 覆盖未提交修改

发现未提交修改时：

1. 先识别哪些文件属于用户原有修改
2. 避免覆盖
3. 采用增量修改
4. 在最终汇报中说明

不要擅自提交 Git commit，除非用户明确要求。

---

## 14. 环境变量与安全

所有环境变量通过：

```text
.env.local
```

或对应部署平台配置。

必须提供：

```text
.env.example
```

`.env.example` 只包含变量名和示例占位，不得包含真实密钥。

不得：

* 将密钥写入源码
* 将密钥提交到 Git
* 在客户端暴露服务端密钥
* 在日志中输出完整 token
* 使用真实个人信息填充示例配置

公开客户端环境变量必须使用明确前缀，例如：

```text
NEXT_PUBLIC_SITE_URL
```

---

## 15. 测试要求

至少检查：

* 首页可以渲染
* 精选项目可以显示
* 项目筛选可以工作
* 项目详情可以通过 slug 访问
* 不存在的项目返回 404
* 博客文章能够读取
* 不存在重复 slug
* 项目必填字段完整
* 导航链接有效
* 构建过程无 TypeScript 错误

在修改完成后运行项目实际存在的检查命令。

常见命令：

```bash
npm run lint
npm run test
npm run typecheck
npm run build
```

不要假设命令存在。先检查 `package.json`。

如果测试失败：

1. 判断是否由本次修改引起
2. 修复本次修改造成的问题
3. 不得删除测试来让检查通过
4. 不得降低断言强度
5. 不得通过跳过测试掩盖错误

---

## 16. 工作流程

每次任务按照以下顺序执行。

### 第一步：理解任务

明确：

* 用户希望改变什么
* 哪些页面受影响
* 是否涉及数据结构
* 是否涉及设计系统
* 是否影响 SEO、测试或构建

### 第二步：检查项目

检查：

* 项目结构
* package.json
* Git 状态
* 已有设计系统
* 相关页面和组件
* 测试文件
* README

### 第三步：制定最小实施方案

优先进行：

* 小范围增量修改
* 复用现有组件
* 保持已有架构
* 避免无关重构

不要为了完成一个局部任务重写整个项目。

### 第四步：实施

实施过程中：

* 保持类型安全
* 同步更新测试
* 同步更新数据类型
* 同步处理移动端
* 同步考虑无障碍
* 不留下无说明的临时代码

### 第五步：验证

至少运行：

* 与修改直接相关的测试
* TypeScript 检查
* lint
* build

如无法运行某项检查，说明原因，不得声称已经通过。

### 第六步：汇报

最终汇报需要包含：

1. 完成了什么
2. 修改了哪些主要文件
3. 设计或架构决策
4. 执行了哪些检查
5. 每项检查的真实结果
6. 仍存在的问题
7. 是否存在待补充内容

---

## 17. 修改范围控制

只修改完成当前任务所必要的文件。

除非任务明确要求，否则不要：

* 全面升级依赖
* 更换框架
* 更换包管理器
* 重做整个设计系统
* 改变项目目录结构
* 删除已有页面
* 修改所有文案
* 格式化整个仓库
* 大规模重命名
* 增加与任务无关的功能

如果发现其他问题，可以在最终汇报中列出，但不要顺手扩大修改范围。

---

## 18. 文案原则

文案应：

* 真实
* 具体
* 克制
* 易于理解
* 避免营销化表达
* 避免夸大项目成熟度

推荐表达：

* “个人项目”
* “教学仿真”
* “答辩演示 MVP”
* “当前以模拟数据为主”
* “预留外部设备接口”
* “正在持续完善”
* “用于验证产品和技术方案”

避免表达：

* “行业领先”
* “革命性”
* “重新定义”
* “顶尖”
* “世界级”
* “生产级”，除非确实满足生产环境要求
* “企业级”，除非有明确架构和验证依据
* “完整实现真实机器人控制”
* “已经应用于工业现场”

---

## 19. 图片与占位内容

项目图片尚未提供时：

* 使用比例统一的本地占位区域
* 标记推荐图片尺寸
* 不要随机引用不相关的网络图片
* 不要使用存在版权风险的图片
* 不要把装饰图当作真实项目截图

推荐项目封面比例：

```text
16:10
```

推荐博客封面比例：

```text
16:9
```

图片路径集中维护，避免散落在 JSX 中。

---

## 20. 完成标准

任务只有在以下条件满足时才算完成：

* 用户要求的功能已经实际实现
* 页面可以正常访问
* 移动端布局可用
* TypeScript 检查通过
* lint 通过
* 相关测试通过
* 生产构建通过
* 没有虚假链接
* 没有虚构个人内容
* 没有覆盖用户原有修改
* 没有新增明显安全问题
* README 或相关文档已按需要更新

如果某项未完成，必须明确写出，不能使用“基本完成”掩盖缺失项。

---

## 21. 最终回复模板

完成任务后使用以下格式汇报：

```text
## 完成结果

简要说明本轮完成的内容。

## 主要修改

- 文件或模块：修改内容
- 文件或模块：修改内容

## 实现效果

- 功能效果
- 页面效果
- 响应式效果
- 无障碍效果

## 验证结果

- TypeScript：
- ESLint：
- Tests：
- Build：

## 保留与限制

- 未修改的内容
- 当前仍为占位的内容
- 已知限制

## 下一步建议

列出最多 3 项与当前项目直接相关的建议。
```

所有验证结果必须来自真实执行，不得推测。
